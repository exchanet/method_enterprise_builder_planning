# ================================
# Method Enterprise Builder Planning
# .gitignore — Enterprise-grade
# ================================

# ──── Node.js Dependencies ────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.pnpm-store/

# ──── Package Manager Lock Files ──────────────────────────────────────
# Mantener package-lock.json para reproducibilidad, pero excluir otros
# package-lock.json  # Descomenta si prefieres no versionarlo
yarn.lock
pnpm-lock.yaml

# ──── Build Outputs ───────────────────────────────────────────────────
dist/
build/
out/
*.tsbuildinfo
*.js.map
*.d.ts.map

# Archivos TypeScript compilados (mantenemos .ts fuente)
# Solo si usas compiled output que no debe versionarse
# packs/**/validators/**/dist/
# packs/**/validators/**/build/

# ──── Test Coverage & Reports ─────────────────────────────────────────
coverage/
.nyc_output/
*.lcov
reports/
test-results/
junit.xml
*.junit.xml

# ──── Environment Variables & Secrets ─────────────────────────────────
.env
.env.local
.env.*.local
.env.production
*.key
*.pem
*.p12
secrets/
credentials/

# ──── IDE & Editor Specific ───────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Cursor local (NO excluir .cursor/rules ni .cursor/skills que son parte del proyecto)
.cursor/chat/
.cursor/projects/
.cursor/.cursorrules  # Si existe, es local

# ──── OS Specific ─────────────────────────────────────────────────────
# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# Linux
.directory
.Trash-*

# ──── Temporary Files ─────────────────────────────────────────────────
*.tmp
*.temp
*.log
*.cache
.cache/
tmp/
temp/

# ──── User-Generated Content (should not be in repo) ──────────────────
# Reportes de análisis generados por usuarios (mantener templates)
ANALISIS-PDCA-T-*.md
CORRECCIONES-APLICADAS-*.md
MEJORAS-COBERTURA-*.md
RELEASE-*.md
REPORTE-*.md

# ADRs de usuarios (mantener solo ejemplos en examples/)
# Descomenta si no quieres versionarlos:
# docs/adr/*.md
# !docs/adr/README.md
# !docs/adr/.gitkeep

# Contexto enterprise generado por usuarios
# docs/enterprise-context.md
# docs/risk-matrix.md
# docs/stride-analysis.md

# ──── CI/CD Artifacts ─────────────────────────────────────────────────
.ci-cd/artifacts/
.ci-cd/cache/
*.artifact

# ──── Backup Files ────────────────────────────────────────────────────
*.bak
*.backup
*.old
*~.md

# ──── Databases & Data Files ──────────────────────────────────────────
*.sqlite
*.sqlite3
*.db
*.mdb

# ──── Compiled Binaries ───────────────────────────────────────────────
*.exe
*.dll
*.so
*.dylib

# ──── Archives ────────────────────────────────────────────────────────
*.zip
*.tar
*.tar.gz
*.rar
*.7z

# ──── Large Files (Git LFS si se necesitan) ───────────────────────────
*.mp4
*.mov
*.avi
*.mkv
*.pdf  # Descomenta si quieres versionarlos

# ──── Excluir instalaciones de usuarios en otros proyectos ───────────
# Si alguien clona el método en su proyecto, estos NO deben versionarse:
.cursor-install/
.method-install/

# ──── Keep Files ──────────────────────────────────────────────────────
# Archivos .gitkeep para mantener directorios vacíos en Git
!.gitkeep
!**/.gitkeep

# ──── Exceptions (force include) ──────────────────────────────────────
# Forzar inclusión de archivos importantes que podrían excluirse por error
!.ci-cd/templates/**/*.yml
!.ci-cd/templates/**/*.yaml
!.ci-cd/scripts/*.sh
!packs/**/validators/**/src/**/*.ts
!packs/**/validators/**/tests/**/*.test.ts
!examples/**/*.md

# ──── Documentation Build ─────────────────────────────────────────────
# Si usas generadores de docs (TypeDoc, JSDoc, etc.)
docs-build/
api-docs/
.docusaurus/

# ──── Package Manager Caches ──────────────────────────────────────────
.npm/
.yarn/
.pnpm/
